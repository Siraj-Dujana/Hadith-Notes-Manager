<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Hadith</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header>
    <h1>Hadith Notes Manager 📜</h1>
  </header>

  <main>
    <section class="form-section">
      <h2>Edit Hadith ✍️</h2>
      <form action="/hadith/<%= hadith._id %>?_method=PUT" method="POST" class="hadith-form">
        
        <label for="hadith_text">Hadith Text</label>
        <textarea name="hadith_text" id="hadith_text" rows="5" required><%= hadith.hadith_text %></textarea>

        <label for="source">Source</label>
        <input type="text" name="source" id="source" value="<%= hadith.source %>" required />

        <label for="topic">Topic</label>
        <input type="text" name="topic" id="topic" value="<%= hadith.topic %>" required />

        <button type="submit" class="btn">Update Hadith</button>
        <a href="/hadiths" class="btn-secondary">Cancel</a>
      </form>
    </section>
  </main>
</body>
</html>
